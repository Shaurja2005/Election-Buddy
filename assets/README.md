# UI Assets

Store website media in the appropriate subfolder:

- `images/` for PNG, JPG, WebP, and other raster images
- `videos/` for MP4, WebM, and other video files
- `svg/` for SVG icons and illustrations

Keep reusable UI assets here. Files that must be served directly by Next.js can also be copied to `public/` when needed.
